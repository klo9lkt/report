모래시계 만들기 코드 설명

*원본에서 일부 코드(#include 등)가 생략되었다.

int n, i, j, k; // n: 사용자에게 받는 수(홀수), i: 한 줄의 * 개수, j: * 반복 생성, k: 공백 반복 생성 역할을 한다.


scanf_s("%d", &n); // 사용자로부터 입력받는다.


for (i = n; i >= 1; i--) // 모래시계 윗부분이므로 i는 감소해야 한다.

{ 

if (i % 2 == 0) continue; // * 의 개수가 짝수일 경우 넘어간다.


for (k = 1; k <= (n - i) / 2; k++) printf(" "); // 공백을 (n - i)/2번 반복한다.

*(n - i)/2: 예를 들어 n=7, i가 7,5,3,1 일 때, (n - i)/2는 0,1,2,3이 되고 이는 곧 그 줄의 공백의 개수가 된다. 


for (j = 1; j <= i; j++) printf("*"); // *을 i번 반복한다.


printf("\n"); // 줄을 나눈다.

}

for (i = 2; i <= n; i++) // 모래시계 아랫부분이므로 i는 증가해야 한다.{


// 이 밑으로는 위와 기능이 같다.

if (i % 2 == 0) continue; 
for (k = 1; k <= (n - i) / 2; k++) printf(" ");
for (j = 1; j <= i; j++) printf("*");
printf("\n");
	
}


